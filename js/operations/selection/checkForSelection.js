/**
 * Megvizsgálja, hogy mely objektumok kiválaszthatóak, feltölti a cache-ket
 * 
 * @returns {undefined}
 */
function checkForSelection() {
    //this.contexts.forEach(context => context.checkForSelection());
}